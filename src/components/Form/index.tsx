import { FormInputContainer } from "./InputContainer";
import FormRoot from "./Root";
import FormTitle from "./Title";

export const Form = {
  Root: FormRoot,
  Title: FormTitle,
  InputContainer: FormInputContainer,
};
